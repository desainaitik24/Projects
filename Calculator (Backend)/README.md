This app handles calculations in backend or server side rather than frontend.
- run "npm install" command to install dependencies (modules).
- add "/bmiCalculator" to current url to calculate BMI calculations.
